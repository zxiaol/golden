# 生产环境配置
server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 生产环境数据库配置（通过环境变量覆盖）
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/ecommerce?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai}
    username: ${SPRING_DATASOURCE_USERNAME:ecommerce}
    password: ${SPRING_DATASOURCE_PASSWORD:ecommerce}
  # 生产环境不显示 SQL
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none

# JWT配置（生产环境，建议使用环境变量）
jwt:
  secret: ${JWT_SECRET:golden-ecommerce-secret-key-2024-prod}
  expiration: ${JWT_EXPIRATION:86400000} # 24小时

# 文件上传配置（生产环境）
file:
  upload:
    path: ${FILE_UPLOAD_PATH:/app/uploads}
    url: ${FILE_UPLOAD_URL:http://localhost:8080/uploads/}

# Flyway 配置（生产环境）
# 如果 Flyway 需要使用不同的数据库用户，可以配置独立的连接信息
# 如果不配置，Flyway 会使用 spring.datasource 的配置
flyway:
  enabled: true
  locations: classpath:db/migration
  baseline-on-migrate: true  # 关键：如果数据库已有表，自动创建基线
  baseline-version: 0  # 基线版本号
  validate-on-migrate: false  # 临时禁用验证，让基线先创建
  clean-disabled: true  # 生产环境禁止清理
  # Flyway 独立数据库配置（如果配置了会覆盖 spring.datasource）
  # 生产环境建议使用专门的迁移用户（具有 DDL 权限）
  # 支持通过环境变量覆盖：FLYWAY_URL、FLYWAY_USER、FLYWAY_PASSWORD
  url: ${FLYWAY_URL:jdbc:mysql://mysql:3306/ecommerce?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=Asia/Shanghai}
  user: ${FLYWAY_USER:ecommerce}
  password: ${FLYWAY_PASSWORD:ecommerce}

# 日志配置（生产环境）
# MyBatis SQL 日志通过日志级别控制（Spring Boot 3.x 推荐方式）
logging:
  level:
    root: INFO
    com.golden: INFO  # 生产环境使用 INFO 级别
    com.golden.mapper: WARN  # 生产环境不显示 SQL
    org.springframework: WARN
    org.mybatis: WARN
    org.springframework.web: WARN
    org.flywaydb: INFO  # Flyway 日志
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{50} - %msg%n"
  file:
    name: ./logs/application-prod.log

