<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.golden.mapper.CartItemMapper">
    <resultMap id="BaseResultMap" type="com.golden.entity.CartItem">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="quantity" property="quantity"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
        <association property="product" javaType="com.golden.entity.Product">
            <id column="p_id" property="id"/>
            <result column="p_name" property="name"/>
            <result column="p_price" property="price"/>
            <result column="p_image_urls" property="imageUrls"/>
        </association>
    </resultMap>
    
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT c.*, 
               p.id as p_id, p.name as p_name, p.price as p_price, p.image_urls as p_image_urls
        FROM cart_items c
        LEFT JOIN products p ON c.product_id = p.id
        WHERE c.user_id = #{userId}
        ORDER BY c.created_at DESC
    </select>
    
    <select id="findByUserIdAndProductId" resultMap="BaseResultMap">
        SELECT * FROM cart_items 
        WHERE user_id = #{userId} AND product_id = #{productId}
    </select>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT c.*, 
               p.id as p_id, p.name as p_name, p.price as p_price, p.image_urls as p_image_urls
        FROM cart_items c
        LEFT JOIN products p ON c.product_id = p.id
        WHERE c.id = #{id}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cart_items (user_id, product_id, quantity)
        VALUES (#{userId}, #{productId}, #{quantity})
        ON DUPLICATE KEY UPDATE quantity = quantity + #{quantity}
    </insert>
    
    <update id="update">
        UPDATE cart_items SET quantity = #{quantity} WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM cart_items WHERE id = #{id}
    </delete>
    
    <delete id="deleteByUserId">
        DELETE FROM cart_items WHERE user_id = #{userId}
    </delete>
</mapper>

