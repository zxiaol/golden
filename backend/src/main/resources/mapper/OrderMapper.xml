<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.golden.mapper.OrderMapper">
    <resultMap id="BaseResultMap" type="com.golden.entity.Order">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="order_no" property="orderNo"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="shipping_address" property="shippingAddress"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO orders (user_id, order_no, total_amount, status, shipping_address)
        VALUES (#{userId}, #{orderNo}, #{totalAmount}, #{status}, #{shippingAddress})
    </insert>
    
    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE id = #{id}
    </select>
    
    <select id="findByOrderNo" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE order_no = #{orderNo}
    </select>
    
    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM orders WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countByUserId" resultType="int">
        SELECT COUNT(*) FROM orders WHERE user_id = #{userId}
    </select>
    
    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM orders
        ORDER BY created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM orders
    </select>
    
    <update id="update">
        UPDATE orders
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="shippingAddress != null">shipping_address = #{shippingAddress},</if>
        </set>
        WHERE id = #{id}
    </update>
</mapper>

