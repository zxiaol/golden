<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.golden.mapper.ReviewMapper">
    <resultMap id="BaseResultMap" type="com.golden.entity.Review">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="rating" property="rating"/>
        <result column="content" property="content"/>
        <result column="images" property="images"/>
        <result column="created_at" property="createdAt"/>
        <association property="user" javaType="com.golden.entity.User">
            <id column="u_id" property="id"/>
            <result column="u_username" property="username"/>
            <result column="u_avatar" property="avatar"/>
        </association>
    </resultMap>
    
    <select id="findByProductId" resultMap="BaseResultMap">
        SELECT r.*, 
               u.id as u_id, u.username as u_username, u.avatar as u_avatar
        FROM reviews r
        LEFT JOIN users u ON r.user_id = u.id
        WHERE r.product_id = #{productId}
        ORDER BY r.created_at DESC
        LIMIT #{offset}, #{limit}
    </select>
    
    <select id="countByProductId" resultType="int">
        SELECT COUNT(*) FROM reviews WHERE product_id = #{productId}
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO reviews (user_id, product_id, rating, content, images)
        VALUES (#{userId}, #{productId}, #{rating}, #{content}, #{images})
    </insert>
</mapper>

